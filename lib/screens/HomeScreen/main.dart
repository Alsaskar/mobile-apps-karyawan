import 'package:flutter/material.dart';
import 'package:pendonor_sukarela/screens/HomeScreen/screen.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Screen(),
    );
  }
}
